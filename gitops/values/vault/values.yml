server:
  ingress:
    enabled: true
    annotations:
      cert-manager.io/cluster-issuer: "letsencrypt-dns"
    ingressClassName: "nginx"
    hosts:
      - host: vault.loukawai.fr
    tls:
      - secretName: vault-tls
        hosts:
          - vault.loukawai.fr
