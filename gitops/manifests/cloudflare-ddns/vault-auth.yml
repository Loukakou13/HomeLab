apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: cloudflare-ddns
  namespace: cloudflare-ddns
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: cloudflare-ddns
    serviceAccount: cloudflare-ddns
    tokenExpirationSeconds: 600
