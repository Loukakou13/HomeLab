apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-certificate
  namespace: gateway
spec:
  secretName: secret-tls
  dnsNames:
    - "*.loukawai.fr"
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
