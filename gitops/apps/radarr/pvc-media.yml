apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-radarr-media-movies
spec:
  storageClassName: "nfs-media"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  volumeName: pv-media-movies-nfs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-radarr-media-downloads
spec:
  storageClassName: "nfs-media"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  volumeName: pv-media-downloads-nfs
