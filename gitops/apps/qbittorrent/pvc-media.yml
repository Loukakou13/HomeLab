apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-qbittorrent
spec:
  storageClassName: "nfs-media"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  volumeName: pv-qbittorrent
