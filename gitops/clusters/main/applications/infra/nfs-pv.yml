apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nfs-pv
  namespace: argocd
spec:
  project: infra
  sources:
    - repoURL: https://github.com/Loukakou13/HomeLab.git
      targetRevision: main
      path: gitops/infra/nfs-pv
  destination:
    server: "https://kubernetes.default.svc"
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
