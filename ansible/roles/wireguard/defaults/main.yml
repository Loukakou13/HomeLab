wireguard_interface: wg0
wireguard_port: 51820
wireguard_network: 10.10.0.0/24
wireguard_server_ip: 10.10.0.1/24
wireguard_listen_address: 0.0.0.0

wireguard_config_dir: /etc/wireguard
wireguard_clients_output_dir: /etc/wireguard/clients

wireguard_dns: 1.1.1.1

wireguard_clients:
  - name: client1
    ip: 10.10.0.2/32
